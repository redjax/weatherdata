[project]
name = "core-utils"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "redjax", email = "no@none.com" }
]
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.3",
]

[project.scripts]
core-utils = "core_utils:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
